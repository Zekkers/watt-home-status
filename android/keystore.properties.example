# Local family sideload signing. Copy to keystore.properties (gitignored)
# and fill in the real values. GitHub Actions uses repo secrets instead.
storeFile=keystore/watt-family.jks
storePassword=
keyAlias=watt-family
keyPassword=
